name: sprouts_manager
description: "manager for eventsprouts events"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.9.0
  cloud_firestore: ^6.4.1
  flutter_riverpod: ^2.6.1
  google_fonts: ^8.1.0
  intl: ^0.20.2
  crypto: ^3.0.7
  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

